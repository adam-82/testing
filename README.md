# test
#Practising with git & github
#How to Clone a public Repository

git clone <source_repository_url>

cd <cloned_repository_directory>

git remote remove origin

git remote add origin <new_repository_url>

git push -u origin master     # this will push the code to the master branch

